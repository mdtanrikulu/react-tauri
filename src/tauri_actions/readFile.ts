export const read_file = (file: any) => {
    // localStorage.setItem('DIR', '/Users/apple/Desktop/untitled')
    // const dir: any = localStorage.getItem('DIR')
    // console.log({dir})
    var data: any = [{name: '', path: ''}]
    // try {
    //     data = require('./' + file)
    // } catch (err) {
    //     console.log({err})
    // }
    return data
}